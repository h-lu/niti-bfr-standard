# AGENTS.md

本文件是进入本仓库的 agent / 工程师入口说明书。

它只固定长期稳定、原则级、方法学级的信息，帮助后续协作者快速理解项目目的、研究对象、A/B/C 三条路线、`quicklook` 与 `formal Af` 的边界，以及算法验证时必须遵守的规则。

本文件不是研究推演笔记，不记录单次实验结论，不记录一次性调参经验，也不把当前某一版代码实现细节误写成长期权威。

## 1. Project Purpose

本项目的目标是为 NiTi BFR 视频分析建立一套可扩展、可解释、可验证的方法框架，至少覆盖两类对象：

- `wire-like`
- `braided`

系统目标是长期保留并比较 `A / B / C` 三条通用分析路线，而不是把项目收缩成：

- 只服务某一张图
- 只适配某一个 demo
- 只剩一条算法还能被讨论

本项目的输出目标是基于“恢复量 vs 温度”曲线计算：

- `Af-95`
- `Af-tan`

这里的核心是构建一个通用、稳健、可解释的 BFR 分析框架，而不是围绕单个样例做图像特化优化。

### 非目标

本项目当前不是为了：

- 针对某一帧或某一条视频做视觉特调
- 追求对单张图的极限拟合
- 把局部调参结果上升为项目级方法学定义
- 将二维视频分析表述成超出其证据边界的高保真仿真或临床结论

## 2. Research Objects

### 2.1 `wire-like`

`wire-like` 指一类细长、单根、主轴清晰、恢复过程主要表现为由弯曲到更直的对象。

其典型特征包括：

- 单根主体
- 近似细长结构
- 主恢复运动方向清楚
- 可以用端点、弦长、中心线、主弯段拟合等几何量刻画恢复过程

### 2.2 `braided`

`braided` 指一类编织网状、主体可由二维投影几何量描述、并存在主轴、宽度分布和区域分布的对象。

其典型特征包括：

- 存在主体功能长度或主轴长度
- 存在沿轴向变化的宽度/直径分布
- 可围绕主体区域、过渡区域、压实区域等几何分区展开分析

### 2.3 共同点与差异

这两类对象共享同一个高层 Af 计算框架：从“恢复量 vs 温度”曲线得到 `Af-95` 与 `Af-tan`。

但二者在图像表征、几何定义和主量构造上并不相同，因此：

- 不能强行共用单一视觉量
- 不能为了统一接口而牺牲对象定义
- 不能把某一类对象的最优表征直接套到另一类对象上

## 3. Core Terms

文档、代码说明和后续 agent 沟通时，默认使用以下术语：

- `object type`：`wire-like` / `braided`
- `analysis route`：`A` / `B` / `C`
- `output mode`：`quicklook` / `formal Af`
- `validation`：`synthetic truth`、`real-video stability`、`cross-route consistency`、`QC sanity`

其中：

- `A/B/C` 表示并行分析路线
- `quicklook` 表示快速查看趋势、稳定性、可见性和 QC 的输出模式
- `formal Af` 表示可进入正式结论的输出模式

重要：`formal Af` 是输出模式，不等于“项目只允许存在一种算法”。

## 4. A / B / C Method Definitions

### 4.1 总原则

`A / B / C` 在本项目里代表三条并行、可比较的通用分析路线。

它们的存在意义包括：

- 提供不同几何视角
- 提供交叉验证
- 提供稳健性对照
- 帮助判断某一对象、某一视频、某一实验条件下哪类恢复量更稳定、更可解释

因此：

- 不允许把文档改写成“只有某一路才算有效”
- 不允许在没有明确架构决策的情况下删掉某一路的项目地位
- 不允许用单图效果好坏来否定某一路作为长期分析视角的存在意义

### 4.2 `wire-like` 的 A / B / C

- `A`：端点 / 弦长 / 位移主导的基线方法
- `B`：主轮廓 / 中心线 / 单弯段拟合主导的方法
- `C`：在 `A/B` 观测基础上的时序增强方法

它们分别回答：

- `A`：对象两端或主位移量恢复了多少
- `B`：对象整体形状或主弯段几何恢复了多少
- `C`：在时间连续性约束下，恢复量是否能更稳定地表达

### 4.3 `braided` 的 A / B / C

- `A`：主轴 / 功能长度类量
- `B`：宽度 / 直径类量
- `C`：投影面积 / 区域积分或等价几何量

在当前实现中，可用下面这组量作为示例理解：

- `A` 示例：`length_axis`
- `B` 示例：`diameter_max`
- `C` 示例：`area_proj`

但这些示例是当前实现映射，不应被误读为“只针对当前某个 demo 的特殊定义”。

对 braided 而言，A/B/C 的长期意义仍然是三类通用几何分析视角：

- 主体长度视角
- 横向宽度视角
- 主体面积或区域积分视角

## 5. Output Modes

### 5.1 `quicklook`

`quicklook` 的作用是：

- 快速检查趋势
- 判断数据是否可用
- 判断分割和几何提取是否稳定
- 输出对照量和 QC 结果

`quicklook` 不是正式结论。

它可以用于：

- 看恢复是否存在
- 看不同路线是否方向一致
- 看对象提取是否稳定
- 看是否值得进入 formal 级分析

### 5.2 `formal Af`

`formal Af` 的作用是：

- 在满足前提条件时，基于恢复量随温度变化曲线给出正式 `Af-95` / `Af-tan`

`formal Af` 的使用前提是：

- 温度同步可用
- 恢复过程覆盖足够完整
- 高温端可判定
- 曲线稳定、可解释、满足对象对应的 formal 准入要求

若这些条件不满足，则必须降级为 `quicklook`，不得强行产出正式 Af。

### 5.3 Display And Selection Policy

无论当前对象是 `wire-like` 还是 `braided`，只要 `A / B / C` 三条路线算出了各自的曲线或温度结果，系统默认应尽量完整展示三路结果，而不是只显示其中一路。

长期展示原则是：

- 默认展示 `A / B / C` 三路的计算结果
- 默认允许用户直接查看、比较并自行选择想参考的路线
- 前端与输出层不需要围绕 `formal Af` 做显示决策
- 不因某一路未被选为默认路线，就在前端或输出层将其隐藏
- 不因某一路 gate 未通过，就把该路线的已算结果做静默屏蔽、替换、降级或抹除
- 可以提示每一路的 QC、稳定性和注意事项，但这些提示不应阻止结果显示

也就是说：

- `display` 的默认原则是三路并列可见
- `selection` 的默认原则是由用户自行判断和选择
- `formal Af` 不应成为前端展示或结果可见性的门槛

## 6. Hard Rules For Agents

以下规则默认对所有进入本仓库的 agent 生效。

### 6.1 禁止单图过拟合

不允许为了让某一张图、某一帧、某一条视频“更好看”而：

- 重定义对象
- 重定义主量
- 重定义 QC 口径
- 重定义恢复率口径
- 把局部调参写成项目级规则

项目优化必须优先服务：

- 通用性
- 稳健性
- 可解释性
- 跨对象类型可延展性

而不是服务某一张图的视觉效果。

### 6.2 禁止把单次实验经验写成长期权威

不允许把以下内容写进项目级定义文档中：

- 某次 demo 的最好结果
- 某个视频上的特定阈值组合
- 某一帧上人工观察得到的经验判断
- 历史上哪一路“当时更推荐”的一次性争论

这些内容如有必要，应保留在实验记录、研究笔记或阶段性总结中，而不是写进本文件这类长期入口说明。

### 6.3 禁止混写 `quicklook` 与 `formal Af`

不允许：

- 把 `quicklook` 结果描述成正式结论
- 把路线计算结果、预览结果或参考结果误写成“formal Af 已成立”
- 把“看起来趋势不错”误写成“已通过 formal 判定”

但这条规则不应被扩写成“未通过 formal 的路线结果不许显示”。

本项目允许：

- 显示 `A / B / C` 三路各自算出的结果
- 由用户在可见结果基础上自行比较和选择
- 在需要时附带 QC 或算法提示信息

本项目不允许：

- 用“formal 未通过”作为隐藏路线结果的理由
- 用“当前默认主路线”作为屏蔽其他路线结果的理由
- 用前端展示策略替代用户对三路结果的判断

### 6.4 禁止越界表述 braided 能力

对于 `braided` 路线，不允许把当前方法表述成：

- `virtual deployment`
- clinical-grade `porosity` 定量
- `apposition` 分析
- 需要三维器械模型、解剖边界条件、商业规划输入的高保真部署预测

当前 braided 路线的合理边界是二维视频几何测量、QC、时序跟踪和方法学验证。

### 6.5 禁止擅自删除 A / B / C 的长期项目地位

不允许在没有明确用户要求或架构决策的前提下：

- 从项目叙述中删除某一路
- 把某一路写成“不再属于本项目”
- 把某一路永久降格为“不值得验证”

可以讨论某一路在当前实现中的成熟度、默认展示优先级或 formal 角色，但不能抹掉其作为长期分析路线的存在。

### 6.6 区分“当前实现状态”与“长期方法学目标”

如果当前代码实现里某一对象、某一输出模式默认只放行某一条 formal 通道，这属于：

- 当前实现状态

而不是：

- 长期方法学定义

`AGENTS.md` 应固定长期原则，不应把某一版实现限制写成永久真理。

同理，如果当前某一版前端或导出逻辑只展示一路结果、弱化其他路线、或对未过 gate 的路线做屏蔽，这也属于：

- 当前实现状态

而不是：

- 长期展示原则

长期展示原则应始终保留三路并列可见、由用户自行判断与选择的空间。

## 7. Validation Principles

验证的目标不是“挑一张图调到最好”，而是判断方法是否：

- 自洽
- 稳定
- 可解释
- 对输入扰动不过度脆弱
- 在多条分析路线之间保持合理一致性

### 7.1 所有对象通用的验证原则

验证必须同时覆盖以下维度：

- `synthetic truth` 是否自洽
- `real-video quicklook` 是否稳定
- `A/B/C` 三路是否给出一致方向的恢复判断
- `Af-95` / `Af-tan` 是否对轻微扰动、阈值扰动和轻度噪声保持可解释稳定

验证时必须优先看：

- 恢复方向是否一致
- 曲线是否单调或近似单调
- 曲线是否存在合理高温端
- QC 是否显示对象提取稳定

而不是只看某一帧轮廓是否“更贴边”。

### 7.2 `wire-like` 的验证原则

`wire-like` 的验证至少应覆盖：

- A/B/C 在 synthetic 上与真值的 Af 误差是否可解释
- A/B/C 曲线形状是否合理
- 真实视频上的几何提取是否稳定
- 真实视频上的单调性和质量分数是否合理
- QC 叠加图是否能支持对端点、中心线、拟合结果的肉眼复核

对 `wire-like` 必须坚持：

- 不因真实视频缺少同步温度或完整高温平台就强行给正式 Af
- 不因某一帧边缘更漂亮就改写长期几何定义
- 不因某一路在单视频上短期波动较大就直接取消其验证意义

### 7.3 `braided` 的验证原则

`braided` 的验证至少应覆盖：

- synthetic、quicklook、formal Af 三条链路使用同一套主体定义
- 同时检查主轴、直径、面积三类量
- 不只优化某一路而忽略其他两路的可解释性
- 主体提取、中心线提取、端部处理和区域定义是否前后一致

对 `braided` 必须检查关键 QC，包括但不限于：

- `body-only` 口径是否自洽
- `attachment leakage`
- `centerline disagreement`
- `endpoint stability`
- 主体区域与附件区域是否被合理区分

对 `braided` 必须坚持：

- 不因某个视频上的端帽、附件、局部阴影而重写主体定义
- 不把局部图像适配行为提升为全局规则
- 不把某一路短期更稳误写成“项目以后只剩这一条”

### 7.4 验证标准的写法要求

在长期文档中，验证标准应写成原则性要求，而不是：

- 一次性的命令列表
- 某次实验的数值阈值表
- 某个 demo 的误差截图
- 某一版脚本的调用顺序

实现层和实验层可以继续维护具体脚本、具体输出、具体阈值，但这些属于执行细节，不属于本文件要固定的长期原则。

## 8. Writing Policy For Future Agents

以后更新项目文档或算法说明时，优先遵守以下写法：

- 先写目标层
- 再写定义层
- 再写规则层
- 最后写验证层

避免写成：

- 研究推演过程
- 一次性实验日志
- 单次调参复盘
- 历史分歧记录

若需要记录这些内容，应放入：

- `README.md`
- `docs/`
- 研究笔记
- 阶段性总结

而不是污染 `AGENTS.md` 这类入口文档。

## 9. Source-of-Truth Links

以下文档用于补充背景与实现现状。若需要更具体的历史、背景或实现细节，请从这里继续阅读：

- [README.md](/Users/wangxq/Documents/niti-bfr-standard-2/README.md)
- [docs/v0.2-scope-guard.md](/Users/wangxq/Documents/niti-bfr-standard-2/docs/v0.2-scope-guard.md)
- [docs/braided-video-formal-method.md](/Users/wangxq/Documents/niti-bfr-standard-2/docs/braided-video-formal-method.md)
- [docs/research-notes.md](/Users/wangxq/Documents/niti-bfr-standard-2/docs/research-notes.md)

阅读顺序建议：

1. 先读本文件，理解长期原则
2. 再读 `README.md`，理解项目入口与现有能力
3. 再读对象相关文档，理解对象边界与方法背景

## 10. One-Page Reminder

如果你是刚进入仓库的 agent，请先记住下面几条：

- 这是一个面向 `wire-like` 与 `braided` 两类对象的通用 BFR 分析项目
- A / B / C 是长期保留并比较的三条分析路线
- A / B / C 的已算结果默认应尽量同时展示给用户
- `quicklook` 不是正式结论
- `formal Af` 必须满足前提条件
- 不能针对单张图过度优化
- 验证必须覆盖 synthetic 与 real-video
- 优先做通用、稳健、可解释的方法，而不是图像特化优化
